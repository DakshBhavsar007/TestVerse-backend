[pytest]
# Place this file at:  backend/pytest.ini
# This tells pytest to add the backend folder to sys.path automatically,
# so `from app.xxx import yyy` always resolves.

testpaths = tests
pythonpath = .
asyncio_mode = auto
