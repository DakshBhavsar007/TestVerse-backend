# Add these to your existing backend/requirements.txt

# JWT Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9     # needed for OAuth2 form login

# Credential encryption (already added earlier)
cryptography>=41.0.0
